<UserControl x:Class="Google_Drive_Organizer_V3.Controls.DeleteButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Google_Drive_Organizer_V3.Controls"
             mc:Ignorable="d"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             d:DesignHeight="100" d:DesignWidth="100">
    <UserControl.Resources>
        <SolidColorBrush x:Key="OriginalColor" Color="#fefe"/>
    </UserControl.Resources>
    <Grid>
        <Ellipse Cursor="Hand" Fill="{StaticResource OriginalColor}">
            <!--<Ellipse.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Ellipse.Triggers>-->
        </Ellipse>
        <svgc:SvgViewbox Source="/icons/trash.svg" Margin="20" IsHitTestVisible="False"/>
    </Grid>
</UserControl>
